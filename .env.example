# AI-Poster Environment Configuration

# Server Configuration
PORT=3000
# HOST=localhost  # Default: localhost (secure, local access only)
# HOST=0.0.0.0    # Use this to allow network access (e.g., from other devices)
                  # WARNING: Only use 0.0.0.0 in trusted networks or with proper firewall rules

# Node Environment (development/production)
NODE_ENV=development

# Future API Configuration (for when backend is added)
# API_KEY=your_api_key_here
# DATABASE_URL=your_database_url_here
